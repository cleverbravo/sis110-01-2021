#include <iostream>
#include <cmath>
using namespace std;
//leer un numero entero
//calcular un numero elevado al cubo y su raiz cuadrada
int main()
{
    cout<<"Introduzca un numero entero:";
    int a;
    cin>>a;
    int c=pow(a,3);//potencia
    double r=sqrt(a);//r=pow(a,0.5);
    cout<<"la potencia es:"<<c<<endl;
    cout<<"la raiz cuadrada es:"<<r;
    return 0;
}
martes miercoles y viernes
